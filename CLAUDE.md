# marketing-os · 프로젝트 헌법

이 파일은 Claude Code가 `marketing-os/` 디렉토리에서 작업할 때 항상 로드됩니다.
모든 에이전트·스킬·명령은 아래 원칙을 따릅니다.

## 프로젝트 정체성

**이름**: marketing-os (마케팅 OS)
**목적**: 1인 마케터가 24시간 가동되는 30개 에이전트 팀을 운영할 수 있도록 하는 Claude Code 기반 자동화 시스템
**강의**: 교육 파트너 「24시간 Full 가동 마케팅 자동화! 30개 에이전트로 완성하는 클로드코드」
**구조**: 트루먼(오케스트레이터) 1 + AI 직원 53 + 보관 14 · 방법론 100 · 스킬 54 · · MCP 8
**쓰는 법**: 외울 문장 **3개** · ①「팀 구축하자」(게이트 6) → ②「팀 실행하자」(게이트 4·시동·대시보드) → ③「팀 피드백하자」(주간·월간)

## 시작 모드·가이드 위임 (msk 연동 · 2026-07-16 결합 배선)

> 이 폴더는 `msk` 심링크로 "marketing 100 skills"(방법론 정본)와 결합돼 있다. 아래 트리거가 오면 **msk의 해당 문서를 읽어 그 절차 그대로** 실행한다 (복사 금지 · 정본은 msk 쪽 하나).

| 트리거 | 실행 |
|---|---|
| **"팀 구축하자"** ⭐ (변형: 마케팅팀 구축하자·우리 회사 팀 만들자·팀 다시 짜자·download team) | **1단계 = download team.** `skills/download/SKILL.md` §1 **게이트 0 + 6개** 실행 (⓪설치 점검·자동 ①지금 어떻게 굴러가나 ②무엇이 막혀 있나 ③실측 ④진단 ⑤편성안 ⑥검증→승인). 게이트마다 멈춰 답을 받는다 · 자동 완주 금지. 승인 후 `brand/my-team.md` + `dashboard.json > deptTeams·deptWork` + 팀장 정의 + **검토층 10 배선**까지 해야 끝. 사람 직원이 여럿이면 게이트 2에서 `skills/download/references/회사셋업.md` 를 심화 참조. |
| **"팀 실행하자"** ⭐ (변형: 팀 켜줘·시동 걸어줘·편성안 배선해줘) | **2단계 = 팀을 켠다.** `skills/팀-실행/SKILL.md` **게이트 4개** (①배선 7곳 ②배선 검증·**대시보드 첫 오픈** ③팀별 사용 카드 ④**첫 업무 시동**). 승인된 편성안을 실제로 도는 팀으로 바꾼다. **끝나는 조건: 팀마다 첫 업무 1건이 실제로 돌아 산출물이 나옴.** "안 돌면 받은 게 아니다." ⚠️ "이 업무 실행해줘" 같은 개별 업무 실행은 이 스킬이 아니다. |
| **"팀 피드백하자"** ⭐ (변형: 우리 팀 잘 돌아가?·팀 점검·주간 점검·월간 회고) | **3단계 = 운영 점검.** `skills/팀-피드백/SKILL.md` 실행. **주간·월간을 자동 판별한다** (마지막 점검 21일 미만 = 주간). **주간** = 실측 ①②③ → 고칠 것 1~2개 · **조직 안 건드림**. **월간** = 실측 6종 + 성장 트리거 → 고칠 것 3개 → ⏸ · 재편성이 답이면 「팀 구축하자」로 넘긴다. |

> **외울 말은 위 3개가 전부다.** 구축 → 실행 → 피드백.
> 아래는 필요할 때만 쓰는 것이라 외우지 않는다 · 변형 표현은 각 스킬 frontmatter 가 알고 있으므로
> **표에 안 적혀 있어도 자연어로 말하면 잡힌다.** 표를 늘리지 말 것.

### 그 밖에 · **파일을 여는 것만** (스킬은 여기 안 적는다)

| 트리거 | 실행 |
|---|---|
| "매뉴얼" / "뭐라고 말해야 해?" | `0.여기서-시작하세요.md` 상황별 문장집 (상황 먼저 묻고 해당 섹션만) |
| "구조 보여줘" | `0.여기서-시작하세요.md` + `구조도.html` 오픈 |

> ⚠️ **스킬은 frontmatter 가 자기 트리거를 갖는다. 표에 안 적어도 잡힌다.**
> 전수 확인 (2026-08-04): `설치하자`·`스킬100-설치`·`download`(직원·스킬 다운로드)·`온보딩` 모두
> frontmatter 에 트리거 문구를 갖고 있어 표 없이 발동한다.
> **여기에 스킬을 추가하지 말 것.** 표가 계속 늘어난 원인이 이것이다 (같은 정보를 두 곳에 둔 것).
> 위 두 줄만 남긴 이유: `0.여기서-시작하세요.md`·`구조도.html` 은 스킬이 아니라 **그냥 파일**이라,
> 이 표가 없으면 어디를 열어야 하는지 알 방법이 없다.

**팀을 안 만들고 방법론 100개만 쓰는 길**도 있다 → **"스킬 100개 설치하자"** (`skills/스킬100-설치/`).
실측상 100개 전부 팀·에이전트 의존이 0이라 팀 없이 그대로 돈다 · `brand/` 가 비면 **[샘플]** 로 돈다.
사람이 읽는 안내는 `0.여기서-시작하세요.md` 맨 위 갈림길에 있다.

- 스킬 실행·게이트·브리핑 규칙 정본도 msk (`SKILL.md` 8단계·`TEAM.md`·`gates/`) · 이 폴더 orchestrator.md·_conventions.md는 그 이 회사 배치판이다
- 단, **브랜드 데이터는 이 폴더의 `brand/`가 정본** (orchestrator §3 규칙 유지)
- **타사 이식(파일럿)**: `brand/org-map.md`(회사 업무 지도 = 조직도+업무감사) + `brand/roster.md`(75명 팀 명단)를 손으로 채워 회사별 팀 구성. 순서·게이트 정본은 `brand/org-map.md`. ⚠️ 자동 편성 스킬은 파일럿 2~3건 검증 후 승격 · 지금은 반자동(사람 개입)

## 협업 프로토콜 (모든 작업 공통 · 이 OS의 핵심 동작)

이 OS는 일반 마케터가 명령어를 몰라도 쓸 수 있어야 한다. 모든 작업에서:

1. **자연어 우선**: 사용자가 자연어로 요청하면 세팅된 장치(스킬→에이전트→체인→MCP)를 트리거 매핑으로 **알아서 발동**한다. 명령어를 외우라고 요구하지 않는다.
2. **시작 전 확인**: 작업 착수 전 결과를 좌우하는 핵심 질문 1~3개를 먼저 묻는다 (이미 답이 프로필·메모리에 있으면 묻지 않고 활용).
3. **방향 제시**: 무작정 실행하지 않고 추천안 + 근거를 짧게 제시한 뒤 진행한다.
4. **결과 검토**: 산출물 완성 시 스스로 검토 요약(무엇을·왜·한계)을 붙인다.
5. **피드백 요청**: 끝에 반드시 피드백을 청한다 ("이 방향 맞나요? 고칠 점은?").
6. **기억**: 받은 피드백·확정된 선호·반복 패턴을 `brand/marketer.md` 성장 로그와 메모리에 적재한다. **같은 걸 두 번 묻지 않는 것**이 성장의 증거다.
7. **장치 제안**: 반복되는 업무를 감지하면 스킬화(skill-builder)·자동화(cron/루틴)를 먼저 제안하고, 필요한 스킬/워크플로/MCP가 없으면 찾아서 제안→설계→구축한다 (구축은 사용자 승인 후).
8. **운영 면**: 로컬은 Claude Code(터미널·데스크탑), 외부는 Discord(트루먼 봇)에서 동일하게 동작한다.

## 구조 정본

구조 설명·숫자 인용의 정본은 **`구조.md`** 다. 먼저 읽고, 바뀌면 거기부터 고친다.

⚠️ **실행 방식은 실행 면이 정한다** (구조.md "두 가지 실행 방식")
- **클로드 코드(대화형) = 게이트** · 멈춰서 답을 받고 다음. 팀 구축·직원 채용·스킬 제작.
- **대시보드(헤드리스) = 라우터** · 되물을 수 없으니 한 번에 완주. 일상 실행.
- 대시보드에서 게이트형 스킬을 부르면 **자동 완주로 바뀐다.** 그래서 구축은 클로드 코드에서 하고,
  대시보드에서는 **편성안까지만** 만들고 ⏸ 로 올린다 (조직을 자동으로 바꾸지 않는다).
- 게이트로 올리는 기준: ①되돌리기 어려운가 ②사람 판단이 2회 이상 필요한가 ③틀리면 뒤 작업이 다 헛되나 · **하나라도 예면 게이트**.

⚠️ **두 축을 섞지 마라.** "구조 보여줘" 요청을 받으면 **어느 축·어느 항목인지 먼저 확인한다.**
- **국면 A·B** — A 구축 전(클로드 코드에서 다운로드) / B 구축 후(대시보드에서 운영)
- **층 ①②③** — ① 도구(팀을 짓고 점검 · 회사 무관) / ② 재료(배치될 부품 · 회사 무관) / ③ 결과(그 회사 팀 · 회사마다 다름)

이 폴더의 5팀 46명은 **③ 결과**지 제품 사양이 아니다. 제품은 ①이다.

## 아키텍처 (Part 10 AX 시스템)

```
사용자 (디스코드 / 터미널)
  ↓
오케스트레이터 (agents/orchestrator.md)  ← 라우팅·기획만, 도구는 최소
  ↓ 자연어 분기
서브에이전트 (agents/part{3~9}-*/...)   ← 도메인 전문가, 각자 MCP 권한
  ↓
산출물 → Discord webhook + Notion 페이지 + 로컬 파일
```

- **현 편성 (2026-08-02 확정)**: **5팀 46명** · 구축실·콘텐츠팀·강의사업팀·성과분석팀·시장·기획팀 + 트루먼. 편성 정본은 `brand/my-team.md`, 배선은 **`brand/dashboard.json > deptTeams`** 한 곳(화면·디스패치가 같이 읽는다 · 2026-08-03 코드 하드코딩 제거). **팀 명단에 없으면 대기다** · 정원은 미리 짓지 않고 6개 성장 트리거로만 늘린다(구축실이 월간 회고에서 관측 → ⏸ 승인). ⚠️ 설정 정본은 Supabase라 `brand/dashboard.json`만 고치면 반영되지 않는다.
- **팀 모드 (실험)**: 동시 협업·토론 검수가 필요한 큰 업무는 에이전트 팀으로 가동 (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 활성화됨). 판단 기준·편성·규칙은 `agents/TEAM-MODE.md` 정본 · 기본값은 서브에이전트, 팀 소집은 사용자 승인 후.

## 하네스: 마케팅 에이전트 팀 (revfactory/harness)

**목표:** 여러 에이전트가 협업해야 하는 큰 마케팅 업무를 팀 편성 → 조율 → 종합 → 게이트까지 한 흐름으로 완주.

**트리거:** 팀 소집·다관점 검수·협업 업무 요청("팀 소집", "팀으로 돌려줘", "토론 검수", "리서치 스웜", 팀 결과 수정·재실행) 시 `team-run` 스킬을 사용하라. 판단 기준·운영 규칙 정본은 `agents/TEAM-MODE.md`. 단건 업무·단순 질문은 기존 라우팅(orchestrator.md)대로 직접 처리.


### 업무 착수 전 팀 판정 (필수 · 2026-07-20 사용자 지시)

모든 업무 요청은 착수 **전에** `agents/TEAM-MODE.md §1` 표로 팀 여부를 판정한다. 판정을 건너뛰지 않는다.

| 업무 성격 | 처리 | 예 |
|---|---|---|
| **기획·전략·검수·리서치·다관점 판단** | **팀 (기본값)** · 편성안 1줄 고지 후 **즉시 소집** (승인 대기 안 함) | 콘텐츠 기획·캠페인 설계·플랜 검수·시장 스캔·원인 규명 |
| **실행·수리·조회·단순 변환** | **단독** · 팀 만들지 않는다 | 파일 수정·경로 복구·동기화·검색·포맷 변환 |
| 애매 | **팀** · 단, 편성 이유를 한 줄 밝힌다 | |

- 편성은 `TEAM-MODE.md §2` 표준 3종(부서 협업팀·검수 토론 패널·리서치 스웜)에서 고른다. 새 편성을 즉흥으로 만들지 않는다.
- 실행은 `skills/team-run`. 도메인 오케스트레이터(`youtube-content-orchestrator`·`campaign-orchestrator` 등)가 있으면 **그쪽이 우선**이다.
- 토큰 소모가 큰 편성(5명 이상)은 고지에 규모를 함께 적는다.

## 도구 우선순위 (위에서 아래로)

1. **스킬 (skills/)** · 반복되는 작업 패턴 (뉴스레터 작성, 브랜드 톤, 리포트 템플릿)
2. **서브에이전트 (agents/)** · 도메인 전문 워크플로 (광고 분석, VoC 분석, GA4 리포트)
3. **MCP** · 외부 시스템 연결 (Notion, GA4, Discord, Buffer, Firecrawl ...)
4. **Bash / WebFetch** · MCP가 없는 작업에만

## 폴더별 역할

| 폴더 | 역할 | 수정 빈도 |
|---|---|---|
| `agents/` | 서브에이전트 정의 (.md) | 강의 진행 중 추가 |
| `skills/` | 재사용 스킬 (SKILL.md) | 강의 진행 중 추가 |
| `discord-bot/` | 디스코드 봇 설정 (webhook URL, 슬래시 명령 정의) | Part 2/Part 10에서 작성 |
| `automation/` | cron/launchd 스케줄러 | Part 10에서 작성 |
| `curriculum/` | 강의 자료 (이론 README + 실습.md + 산출물 예시) | 강의 진행 중 작성 |

## 명명 규칙

- **에이전트 이름**: kebab-case, 영문, 동사+명사 (예: `competitor-monitor`, `ga4-html-report`)
- **스킬 이름**: kebab-case, 영문 (예: `newsletter-writing`)
- **슬래시 명령**: `/<동사>-<목적어>` (예: `/research-competitor`, `/weekly-report`)
- **산출물 파일**: `{날짜}-{종류}.{ext}` (예: `2026-05-15-meta-roas-report.html`)

## 산출물 표준

### 데이터 출처 표기 (전 스킬 공통 · 예외 없음)

모든 산출물은 **첫 줄에 데이터 출처를 표기한다.**

| 태그 | 언제 붙이나 |
|---|---|
| **[실데이터]** | `brand/` 의 내 정보와 내가 넣은 파일로 만든 것 |
| **[샘플]** | `sample-data/` 의 A 브랜드 데이터로 만든 것 |
| **[추정]** | 출처를 확인하지 못해 팀이 추정한 수치 (그 줄·그 칸에만) |

### 데이터가 없을 때 · 한 번 묻고 샘플로 간다 (2026-08-04 지시)

데이터가 필요한 스킬인데 쓸 데이터가 안 왔으면 **시작 전에 한 번 묻는다.**

```
이 업무는 〈리뷰 데이터〉가 필요합니다.
  1. A 브랜드 샘플로 진행       ← Enter (기본값)
  2. 내 파일을 올리겠다          ← 경로나 파일을 주세요
  3. 내 brand/ 정보로 진행       ← brand/profile.md 가 채워져 있을 때만
```

- **답이 없거나 애매하면 1번으로 간다.** 묻되 멈추지 않는다.
- 쓸 샘플은 `sample-data/` 에 이미 있다 (리뷰 200건·메타광고 30일·채널성과 90일·경쟁사 3곳·상세페이지·매출·네이버광고). **새로 지어내지 않는다.**
- 진행하면 산출물 첫 줄에 **[샘플]** 과 **어느 파일을 썼는지**를 함께 적는다.
  `[샘플] sample-data/A브랜드-리뷰-200건.csv 로 작성`
- **발화에 파일이 이미 지정됐으면 묻지 않는다** (`sample-data/경쟁사-3곳.md 로 분석해줘`).
- **되물을 수 없는 자리**(대시보드·cron·디스코드 자동 실행)에서는 묻지 않고 1번으로 간다.
  그 대신 산출물에 "물어볼 수 없어 샘플로 진행했다"를 한 줄 밝힌다.

> 왜 바꿨나 · 2026-08-04
> 전에는 "묻지 않고 샘플로 진행"이었다. 멈추지 않는 건 좋았지만 **독자가 자기 데이터로 도는 줄 알았다.**
> `[샘플]` 태그는 붙었어도 결과를 먼저 보면 놓친다. 한 번 묻는 것으로 그 오해를 없앤다.
> 기본값을 샘플로 둔 이유는 그대로다 · **안 돌면 받은 게 아니다.**

- `brand/profile.md` 가 비어 있으면 위 물음의 3번은 감춘다.
- **[추정]** 이 붙은 숫자는 그대로 의사결정에 쓰지 않는다. 근거를 확보하면 태그를 뗀다.
- 지어낸 수치에 "실측"이라고 쓰지 않는다.
- **태그 없이 산출물을 내보내는 것은 규격 위반이다.**

> 이 규칙은 스킬마다 적지 않는다. 회사 규칙에 한 번 쓰면 100개 전부가 따른다.


모든 에이전트의 최종 산출물은 **다음 3곳 중 하나 이상**에 도달해야 합니다:
1. **Discord 채널** · 짧은 요약 + 링크 (webhook 사용)
2. **Notion 페이지** · 전문 + 인사이트 (장기 보관)
3. **로컬 파일** · `outputs/{날짜}/{에이전트명}/` (감사용)

## 보안 원칙

- `.env`는 절대 커밋하지 않음 (`.gitignore`에 등록됨)
- **자격증명은 경로가 아니라 파일명으로 막는다** (`**/token.json`) · 폴더를 옮기면 경로 규칙이 새어 나간다
- 광고 집행 권한이 있는 에이전트(`meta-ads-analyzer` 등)는 **읽기 전용 토큰**을 기본값으로 사용
- 광고 예산 변경·콘텐츠 발행은 **사용자 승인** 단계를 반드시 거침 (디스코드 reaction 또는 명시적 명령)

## 작업 시 우선 확인 파일

- `README.md` · 학습자 진입점
- `.mcp.json` · 어떤 MCP가 활성화되어 있는지
- `agents/{name}.md` · 해당 에이전트의 워크플로
- `curriculum/part{NN}-*/README.md` · 해당 Part의 학습 목표

